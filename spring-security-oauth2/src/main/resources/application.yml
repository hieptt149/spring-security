spring:
  application:
    name: spring-security-oauth2
  security:
    user:
      name: ${SECURITY_USERNAME:eazybytes}
      password: ${SECURITY_PASSWORD:12345}
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID:Ov23lic52iYVO5U4Pl34}
            client-secret: ${GITHUB_CLIENT_SECRET:3b2e69dfb13888299d9bd71bec61222513adfa42}
          facebook:
            client-id: ${FACEBOOK_CLIENT_ID:575898368783351}
            client-secret: ${FACEBOOK_CLIENT_SECRET:7db85fe68c75a6d7e348574a26a8a902}

logging:
  level:
    org:
      springframework:
        security: ${SPRING_SECURITY_LOG_LEVEL:TRACE}
  pattern:
    console: ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}
